# 豆包账号池

豆包账号池是一款基于 Electron 的本地桌面工具，用于管理多个相互隔离的豆包登录环境，并通过本地 HTTP API 为无限画布或其他工作流提供视频生成能力。

当前修复版本：`0.1.72`

## 版本策略

- `v0.1.30`：稳定回退版，保留在 Git 标签和 Releases，不覆盖。
- `v0.1.36`：稳定回退版，包含 Mini/Fast 模型切换修复，保留在 Git 标签和 Releases，不覆盖。
- `v0.1.45`：识别豆包“安全确认”弹窗并仅点击弹窗内确认；识别完成消息容器中的新版视频卡片，并延长准确对话结果渲染等待。
- `v0.1.46`：兼容安全确认弹窗使用 `div/span` 伪按钮的页面结构，只在授权文案容器内点击严格匹配的“确认”。
- `v0.1.47`：过滤豆包时间/会话标题等页面壳文字，识别“已提交视频生成任务”确认文案；安全确认点击后不再把授权弹窗残留文字当成失败回复，并补充“生成建议”识别。
- `v0.1.48`：兼容豆包将“确认生成”渲染为普通 `div/span` 的确认动作，并识别“确认后我再开始生成视频”等生成前建议，避免已进入确认流程却被判定为失败。
- `v0.1.49`：识别豆包返回的模型、比例、素材、画面推进、画幅和分镜等结构化视频脚本，并自动进入明确生成流程。
- `v0.1.50`：识别“预计等待 N 分钟、视频生成好后主动发送、本次生成将消耗每日免费额度”等已接受状态，避免已提交任务被误判失败并提前退回额度。
- `v0.1.51`：识别豆包连续追问视频细节的建议回复，最多追加一次“直接生成”，避免第二次建议回复被误判为提交失败。
- `v0.1.52`：补充识别无问句的分镜片段、画面说明和时间段建议回复，继续走有限的明确生成流程。
- `v0.1.53`：识别“故事段/视频生成提示词”和“0-3 秒、3-7 秒分节拍动作”等近期真实建议回复格式。
- `v0.1.54`：对连续建议/代码回复增加最后一次“确认生成”动作，仍限定最大追加次数，避免中间回复直接终止或无限重试。
- `v0.1.57`：接收并保存 `aspectRatio`，在选择 Seedance 模型后自动设置并校验豆包画幅，日志显示模型、画幅和时长。
- `v0.1.58`：兼容豆包实际显示的“自动 · 10s”画幅/时长设置入口，打开设置后再选择并复核 `9:16` 或 `16:9`。
- `v0.1.59`：画幅选择限定在视频工具栏及其比例弹窗中，并以折叠后的工具栏摘要作为最终校验，修复 `9:16` 切换到 `16:9` 时误点或误判。
- `v0.1.60`：用视频编辑器状态和正式会话 ID 驱动执行流程；兼容账号落在普通对话页、画幅入口文案变体，并停止把输入框清空误判为已提交。
- `v0.1.65`：修复新版豆包视频卡片预热后被 `Esc` 关闭悬停分享工具栏的问题，保持悬停状态再定位分享按钮；延续单视频无“全选”时的“复制链接”识别和“确认生成”优先策略。
- `v0.1.71`：兼容动态渲染的豆包 thread/share 分享页；分享地址只作为内部解析输入，最终必须取得并验证真实 MP4 才返回成功。
- `v0.1.72`：整理无限画布接入约定和发布边界；明确建议由 AI/Codex 等客户端调用本地 API，按 `requestId` 轮询或接收回调，最终只把已验证的 MP4 交给画布。
- `v0.1.64`：修复单视频分享面板没有“全选”时被误判为未打开，稳定重试“复制链接”并继续去水印；提示词建议回复改为优先发送“确认生成”，失败后再使用备用明确生成指令。
- `v0.1.63`：接口日志支持终止等待中或执行中的假任务，立即释放账号并以幂等方式退回本地预扣额度。
- `v0.1.62`：保留可识别的系统测试记录，首页增加按本地日期统计的“今日已生成”数量。
- `v0.1.61`：模型识别改为输入框附近的空间定位，修复豆包 DOM 层级变化及重复文本导致 Mini/Fast 明明可见却判定为未识别。
- `v0.1.44`：覆盖豆包新增的“需要我帮你”“继续写分镜”“可直接复制给 AI 视频工具”等相关回复，并自动追加明确生成指令。
- `v0.1.43`：结果恢复优先锁定任务已记录对话 75 秒，仅在必要时检查同账号最近 3 条对话；执行窗口标题增加任务短 ID。
- `v0.1.42`：识别豆包返回的配音文案、分镜表和优化提示词，自动发送明确的直接生成指令；首次分享页未同步视频时自动回到原会话恢复结果。
- `v0.1.41`：处理豆包新增“确认生成”二次确认按钮；识别提示词建议后自动继续提交并等待真实视频状态。
- `v0.1.40`：修复豆包页面导航和“下载电脑版”提示被误识别为助手回复的问题；已发送但没有真实失败回复时继续等待视频。
- `v0.1.39`：修复豆包确认文案换行导致的误判；已确认提示词发出但未识别标准文案时继续等待，不重复提交、不退回额度。
- `v0.1.38`：新增豆包新版提交确认、违规原文回传和“仍在生成”安全等待。

## 主要功能

- 多账号隔离：每个账号使用固定的 Electron `persist:` partition，Cookie、LocalStorage、缓存和登录状态互不混用。
- 登录持久化：关闭软件后保留各账号登录状态，下次启动可继续使用。
- 共享额度管理：默认每个账号每日 10 点额度，Mini 消耗 2 点，Fast 消耗 3 点，均可在配置页调整。
- 账号池调度：自动选择已登录、空闲且额度充足的账号执行请求。
- 后台视频生成：支持文本提示词和参考图片，跟踪等待、执行、成功和失败状态。
- 严格结果输出：只有取得经过验证的真实 MP4 地址或本地 MP4 文件后，任务才会返回 `success`。
- 可选去水印：可配置第三方解析接口；只在取得并验证 MP4 后返回 `success`，解析失败或未取得 MP4 时返回 `failed`。
- 本地接口服务：默认监听 `127.0.0.1:17888`，支持 API Key、状态查询和回调地址。
- 可观测界面：提供账号概览、剩余额度、Mini/Fast 预计产能、搜索筛选和接口日志详情。
- 行动日志：逐步记录账号、任务、分享地址、去水印和错误信息，默认保留 3 天并自动清理。
- 结果恢复与诊断：支持从账号最近对话恢复已生成视频，日志显示分享复制、去水印重试和耗时信息。
- 跨平台安装包：提供 macOS Apple Silicon DMG 和 Windows x64 EXE。

## 下载与安装

请从 [Releases](../../releases/latest) 下载当前版本：

- macOS Apple Silicon：下载 `0.1.72` ARM64 DMG 或 ZIP。
- Windows x64：下载 `0.1.72` Windows x64 EXE。

当前构建环境没有 Developer ID 证书，因此 macOS 安装包未进行 Developer ID 签名或公证；首次运行若出现来源提示，可进入“系统设置 > 隐私与安全性”，在安全性区域选择“仍要打开”。Windows 安装包也未使用商业代码签名，请核对发布页中的 SHA-256。

## 快速开始

1. 安装并启动应用。
2. 点击“添加账号”，在打开的独立豆包窗口中完成登录。
3. 返回账号池执行检测，确认账号显示为“已登录”和“空闲”。
4. 在“配置管理”中设置 API Key、模型额度、执行策略、去水印 Token 和输出目录。
5. 从无限画布或其他本地工作流调用生成接口。

## 无限画布接入建议

建议使用 AI/Codex 等能够读取项目文档、调用本机 HTTP API 的客户端连接无限画布和账号池。推荐让客户端调用账号池接口，不要让客户端直接操作豆包窗口、系统剪贴板或复制 Cookie。

推荐流程：

1. 先启动豆包账号池，确认 `http://127.0.0.1:17888/health` 正常。
2. 让无限画布或 AI/Codex 客户端调用 `POST /api/generate`，保存返回的 `requestId`。
3. 画布优先使用 `callbackUrl` 接收状态；没有回调服务时，每 3 到 5 秒轮询 `GET /api/requests/{requestId}`。
4. 只有 `status: "success"` 且存在已验证的 `cleanVideoUrl` 或 `.mp4` `outputVideoPath` 时，才把结果写入画布。
5. 不要把 `doubao.com/chat/...`、`doubao.com/thread/...` 或豆包分享页地址当成画布视频结果；这些地址只在账号池内部用于恢复和解析。

连接时建议先阅读 [新手使用手册](docs/USER_GUIDE.md) 和 [本地 API 文档](docs/API.md)，并在画布任务中保留 `requestId`、模型、画幅、来源和错误 `message`，便于重试和排错。账号登录状态、Cookie、分区、SQLite 数据和本地输出文件只保存在本机，不上传 Git。

## 本地 API

默认地址：`http://127.0.0.1:17888`

```bash
curl -X POST http://127.0.0.1:17888/api/generate \
  -H "Authorization: Bearer local-doubao-key" \
  -F "model=seedance_2_0_mini" \
  -F "prompt=生成一段 10 秒科普视频" \
  -F "referenceImage=@/path/to/reference.png" \
  -F "callbackUrl=http://127.0.0.1:3000/doubao/callback"
```

提交成功会先返回 `accepted`，可通过下面的接口查询：

```bash
curl http://127.0.0.1:17888/api/requests/doubao-xxxxxxxxxxxxxxxx \
  -H "Authorization: Bearer local-doubao-key"
```

成功结果只包含最终可用的视频字段：

```json
{
  "requestId": "doubao-xxxxxxxxxxxxxxxx",
  "status": "success",
  "message": "视频生成完成，去水印 MP4 地址已验证（耗时 X 秒，第 N 次解析）",
  "model": "seedance_2_0_mini",
  "cleanVideoUrl": "https://example.com/video.mp4",
  "outputVideoPath": null
}
```

完整接口说明见 [docs/API.md](docs/API.md)。

第一次使用、连接无限画布和排错请先阅读 [新手使用手册](docs/USER_GUIDE.md)。

## 数据位置

运行数据不会提交到仓库：

- macOS：`~/Library/Application Support/doubao-account-manager/`
- Windows：`%APPDATA%/doubao-account-manager/`

SQLite 数据库、账号 partition、Cookie、缓存、上传参考图和登录状态均保存在对应系统用户目录。

## 本地开发

环境要求：Node.js 20 或更高版本。

```bash
npm ci
npm run dev
```

常用命令：

```bash
npm run typecheck  # TypeScript 检查
npm run build      # 生产构建
npm run dist       # macOS 安装包
npm run dist:win   # Windows x64 安装包
```

## 技术栈

- Electron
- Vue 3
- TypeScript
- SQLite / better-sqlite3
- Vite
- electron-builder

## 使用边界

本项目不提供自动注册、验证码处理、账号限制绕过或平台风控规避。请遵守豆包及相关第三方服务的使用规则，仅在本人拥有权限的账号和内容上使用。

本项目是非官方本地工具，与豆包或字节跳动不存在隶属、授权或合作关系。

## 更新记录

版本变化见 [CHANGELOG.md](CHANGELOG.md)。
