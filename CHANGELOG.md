# 更新日志

本文件记录豆包账号池的重要功能变化。

## 0.1.72 - 2026-09-24

### 发布整理

- 将当前源码和文档版本推进到 `0.1.72`，保留 `0.1.30` 和 `0.1.36` 作为稳定回退标签，并继续保留 `0.1.38` 之后的历史版本。
- 补充无限画布接入说明：建议使用 AI/Codex 等能调用本地 HTTP API 的客户端，保存 `requestId`，通过回调或轮询接收结果。
- 明确结果边界：画布只接收经过验证的去水印 MP4 地址或本地 `.mp4` 路径；豆包聊天页、thread/share 分享页只作为内部恢复和解析输入，不作为成功结果返回。
- 明确发布数据边界：账号分区、Cookie、LocalStorage、SQLite、参考图、视频输出和 Token 等运行数据只保存在本机，不提交到 Git。

### 当前已验证

- 类型检查、生产构建和 40 项单元测试在发布前重新执行。
- 近期 6 条历史任务已恢复为成功并取得经验证的 MP4，已通过画布 API 定向回填；没有准确豆包会话地址的任务不会凭相似提示词串单恢复。

### 使用建议

- 无限画布端保存每个任务自己的 `requestId`，不要用提示词、对话标题或账号名称识别任务。
- 参考图任务应通过 API 上传文件或传入账号池所在电脑可访问的路径/URL；不要让外部客户端读取或复制账号 Cookie。

## 0.1.71 - 2026-09-23

### 修复

- 兼容豆包动态渲染的 thread/share 分享页：页面壳没有直接返回视频资源时，不再提前判定复制失败。
- 复制结果仍只接受 thread/share 地址，最终成功条件改为去水印接口返回并验证可播放 MP4；未取得 MP4 仍然失败，不会回传画布。
- 操作日志明确记录“已复制分享地址，等待去水印接口验证真实 MP4”，避免把分享页校验误认为视频已完成。

## 0.1.70 - 2026-09-23

### 修复

- 修复豆包新版视频卡片已经生成，但分享操作栏位于视频元素外层导致无法打开分享面板的问题。
- 后台会真实悬停当前视频卡片，并优先定位新版视频操作栏的分享图标；不再依赖视频元素内部存在分享按钮。
- 保留页面内 `navigator.clipboard` 捕获和分享页视频资源校验，未取得有效分享地址不会进入去水印或返回成功。

## 0.1.69 - 2026-09-23

### 修复

- 修复没有记录正式对话地址时的历史对话恢复排序：先按提示词相关性排序，再取候选，避免真正生成视频的对话被前 3 个 DOM 链接截掉。
- 历史恢复候选从 3 条扩大到 10 条，只用于结果恢复，不会重新提交视频。

## 0.1.68 - 2026-09-23

### 修复

- 扩展豆包新版视频卡片定位：即使封面地址不含 `video` 字样，也会识别 `block-video`、`video-card` 结构并真实点击卡片后再打开分享工具栏。

## 0.1.67 - 2026-09-23

### 修复

- 修复新版豆包视频卡片已生成但分享面板未打开的问题：滚动豆包内部内容容器到最新结果，并在尚未渲染 `<video>` 时真实点击当前视频卡片。
- 优先识别新版视频卡片分享工具栏的图标入口，再进入“复制链接”页面捕获流程。

## 0.1.66 - 2026-09-23

### 修复

- 分享链接采集不再写入或读取 Electron 进程级系统剪贴板。
- 在豆包页面内拦截 `navigator.clipboard.write` 和 `writeText`，兼容 `ClipboardItem`，只接受 `thread/share` 分享地址。
- 复制后页面没有捕获到有效分享地址时直接失败，不把系统剪贴板中的旧内容或 `chat` 页面地址当成结果。
- 保留真实分享页视频资源校验，校验通过后才进入去水印流程。

## 0.1.65 - 2026-09-22

### 修复

- 豆包新版视频卡片的分享工具栏依赖鼠标悬停显示；预热卡片后不再发送 `Esc` 关闭工具栏，改为保持悬停状态再定位并点击分享入口。
- 保留 `0.1.64` 的单视频分享面板识别：可见“复制链接”即可继续，不要求必须出现“全选”。

## 0.1.64 - 2026-09-22

### 修复

- 修复单视频分享面板没有“全选”控件时被误判为“未打开分享面板”的问题。只要当前可见的“复制链接”控件存在，就认为分享面板已打开；仍然要求复制出的地址是 thread/share 分享地址，并通过分享页视频资源校验后才进入去水印。
- 调整提示词建议兜底顺序：第一次优先发送“确认生成”，未启动任务时再发送完整的直接生成指令，最后才使用“直接生成”。
- 近期失败记录确认的根因是视频卡片已生成但分享链接阶段失败，未进入 nologo 去水印请求；本版本不把分享链接失败误写成去水印接口失败。

## 0.1.63 - 2026-09-05

### 新增

- 接口日志和任务详情增加“终止并退还”操作，仅对等待中或执行中的任务开放。
- 新增 `POST /api/requests/{requestId}/stop`，供无限画布或其他本地调用方终止假任务。

### 处理规则

- 排队任务从执行队列移除；执行中任务关闭对应豆包窗口，并阻止后续异步结果覆盖“已停止”状态。
- SQLite 以事务停止任务、释放账号并退回任务实际预扣额度；重复调用不会重复退还。
- 若任务已经提交到豆包，只能停止本地等待和退回本地统计额度，无法撤回豆包平台上的生成。

## 0.1.62 - 2026-09-05

### 改进

- 首页增加“今日已生成”统计，按本机自然日直接查询 SQLite，不受接口日志仅加载最近 100 条的限制。
- 仅统计状态成功且已保存真实视频结果的任务；系统测试任务使用 `system-test` 来源保留，便于与画布正式任务区分和复查。

### 验证

- 恢复并保留 `0.1.61` 的真实端到端成功测试记录 `doubao-85d503a44b664da3`，结果恢复验证未重新生成、未重复扣除额度，并取得去水印 MP4。

## 0.1.61 - 2026-09-05

### 修复

- 视频模型状态按当前输入框附近的工具栏位置识别，不再依赖固定的 DOM 祖先层数。
- 修复同时读取 `innerText` 和 `textContent` 导致模型文案被重复拼接的问题。
- 普通对话页点击“视频生成”后等待模型工具栏真正出现；首次失败时重置到新对话再重试。
- 模型选项只在同时包含 Seedance 2.0 Mini/Fast 的可见菜单内精确选择。
- 点击选项后必须等待工具栏确认目标模型，未确认时不继续设置画幅或提交。

### 验证

- 增加模型工具栏严格文案测试，普通生成回复中的模型名称不会被当成模型入口。
- 类型检查、38 项单元测试和差异检查通过。
- 使用近期模型未确认任务的原提示词，在原问题账号 006 完成真实端到端回归：确认 Mini 和 `16:9` 后提交，豆包生成成功，分享资源验证通过，去水印第 1 次解析取得真实 MP4。

## 0.1.60 - 2026-09-04

### 修复

- 视频模式改用精确、原生点击；模型未识别时先重置到新对话再进入视频生成，避免部分账号停留在普通对话页。
- 模型和画幅状态只从输入框所属编辑器工具栏读取，不再从历史消息或提示词正文猜测。
- 兼容 `16:9`、`画面比例：16:9`、`比例 16:9 · 10 秒` 等画幅入口文案，同时要求入口可交互且位于编辑器内。
- 删除“页面变化或输入框清空即视为已提交”的弱判断；未见正式生成确认时立即停止并退回预扣额度，不再空等完整生成超时时间。
- 只记录非 `local_` 的正式豆包会话；结果恢复优先以正式会话 ID 为任务身份，即使长提示词未完整渲染也会检查该会话的视频状态。
- 历史回退忽略本地草稿会话，其他最近对话仍必须匹配原提示词，避免串单。

### 验证

- 新增正式会话、弱提交证据和画幅文案变体回归测试；类型检查、38 项单元测试和差异检查通过。

## 0.1.59 - 2026-09-04

### 修复

- 画幅入口只匹配豆包视频工具栏的“比例 · 时长”摘要，避免误点提示词、历史消息或弹窗选项。
- 目标比例只在已打开且同时包含“比例、自动、9:16、16:9”的设置弹窗中精确点击。
- 点击后必须等待工具栏摘要变成目标比例才算确认成功；弹窗中的选中状态不再作为成功依据。
- 每次尝试前关闭残留弹窗，最多重试三次；失败信息记录入口、选项和点击后的实际状态。
- 任务日志记录画幅切换前后状态及成功尝试次数。

### 验证

- 补充 `16:9 · 10s` 工具栏摘要及普通正文不应被识别为入口的测试。
- 类型检查、36 项单元测试和差异检查通过。
- 使用当天真实失败提示词回归任务 `doubao-325be621f7974470`：豆包工具栏成功切换并确认 `16:9` 后才提交；豆包随后返回内容违规拒绝，程序正确回传原始原因并退回本地预扣额度。

## 0.1.58 - 2026-09-03

### 修复

- 兼容豆包视频设置入口显示为“自动 · 10s”的页面结构；先打开设置菜单，再选择目标 `9:16` 或 `16:9`，避免两个比例都被误报为“未识别”。
- 保留发送前画幅复核和不一致时退回预扣额度的保护。

### 验证

- 使用近期真实失败提示词复现确认原问题，失败原因为设置菜单未打开。
- 增加“自动 · 10s”入口回归测试；类型检查、单元测试和差异检查通过。
- 本机真实回归任务 `doubao-81cb86197c6749be` 已成功进入豆包生成、复制视频分享地址，并完成去水印 MP4 校验；去水印服务第 3 次解析成功。

## 0.1.57 - 2026-09-03

### 新增

- `/api/generate` 接收并持久化 `aspectRatio`，支持 `9:16` 和 `16:9`。
- 选择 Seedance 模型后自动打开豆包画幅设置并点击目标画幅。
- 发送前重新读取豆包当前画幅；未识别或与目标不一致时停止，不提交视频。
- 任务进度日志增加模型、画幅和时长信息。
- 时长日志在没有显式“总时长”标签时优先取最后一个完整时长标记，避免把分段动作中的“留 1 秒”误记为总时长。

### 验证

- API/任务类型、数据库迁移、画幅文本识别和非法/双画幅不猜测测试已补齐。
- `npm run typecheck`、`npm run test:unit` 和 `git diff --check` 通过。
- 已用真实用户提示词和参考图完成回归：任务 `doubao-e90f0c6801c7483f` 收到并保存 `9:16`，豆包窗口选择并复核 `9:16`，最终去水印 MP4 为 `720x1280`，复制分享地址和去水印均成功。
- 已验证画幅复核保护：页面仍为 `16:9` 时任务在提交前失败并退回预扣额度，不会继续生成。

## 0.1.56 - 2026-09-03

### 修复

- 将“已提交生成任务、生成任务已提交、正在渲染、等待渲染完成”等豆包异步状态识别为已提交并继续等待。
- 当异步状态后带有“视频生成需要多长时间、可以添加音乐吗”等追问时，不再误识别为提示词建议，不再追加指令后误退预扣额度。
- 主进程和页面脚本统一使用同一组异步状态判断规则。

### 验证

- 使用近期真实失败回复“已提交生成任务，等待渲染完成……视频生成需要多长时间……”和“正在渲染，请稍候”加入回归测试。
- `npm run typecheck`、`npm run test:unit` 和 `git diff --check` 通过。
- 使用近期失败任务的原始提示词和参考图完成一次真实端到端测试：豆包提示词建议追加一次明确指令后正式受理，等待生成、复制分享地址、去水印 MP4 验证均成功。

## 0.1.55 - 2026-09-01

### 修复

- 去水印前只接受豆包 `/thread/` 或 `/share/` 分享地址，不再把 `/chat/` 普通对话地址发送给 nologo。
- 当复制结果是普通对话地址时，日志明确提示链接类型不受去水印接口支持。
- 本机去水印配置已更新为用户提供的新 Token；未记录 Token 内容。

### 验证

- 新 Token 请求真实 `/thread/` 地址返回 MP4；请求 `/chat/` 地址返回平台暂不支持，行为与供应商限制一致。

## 0.1.54 - 2026-08-31

### 修复

- 连续收到提示词、分镜或“已生成代码”回复时，依次尝试完整提交指令、“直接生成”和最后一次“确认生成”。
- 追加次数严格封顶，只有仍没有 Mini/Fast 正式提交确认才判定失败。

### 验证

- 使用近期用户真实失败提示词复测连续建议回复路径。

## 0.1.53 - 2026-08-31

### 修复

- 识别豆包返回的“故事段 02、视频生成提示词、竖屏 9:16”等短格式建议。
- 识别“0-3 秒、3-7 秒、7-10 秒”分节拍动作和音频说明，继续进入有限的明确生成流程。

### 验证

- 增加近期三类真实失败回复的回归用例。

## 0.1.52 - 2026-08-31

### 修复

- 识别豆包返回的“7-10s（收尾）”“画面/镜头/人物/卡片”等无问句分镜片段，不再误报为没有提交确认。
- 与连续追问处理共用有限重试，只继续发送一次“直接生成”，避免无限追加。

### 验证

- 增加无问句分镜片段回复的回归用例。

## 0.1.51 - 2026-08-31

### 修复

- 识别“穿着有什么要求、是否添加音乐、拍摄角度有哪些建议”等视频生成前追问为提示词建议。
- 第一次追加完整提交指令后，若豆包仍返回生成细节问题，最多再发送一次“直接生成”；仍无正式提交确认才判失败。

### 验证

- 增加连续追问回复的回归用例。

## 0.1.50 - 2026-08-31

### 修复

- 识别 Mini/Fast 的标准异步提交文案，包括“预计等待 N 分钟”“视频生成好后主动发送”和“本次生成将消耗每日免费额度”。
- 追加明确生成指令后，即使回复抽取结果包含标准等待文案，也继续进入视频结果轮询，不再误判失败或提前退回预扣额度。

### 验证

- 增加标准 Mini 提交文案、Markdown 加粗模型名和“视频生成好后”非完成状态的回归用例。

## 0.1.49 - 2026-08-31

### 修复

- 识别豆包返回的“模型/时长/比例/素材/画面推进/画幅/风格/分镜”结构化视频脚本，不再把它当成普通回复直接失败。
- 对结构化视频脚本自动追加明确生成指令，并继续等待正式的 Mini/Fast 提交状态和真实视频结果。

### 验证

- 增加近期真实失败回复回归用例。

## 0.1.48 - 2026-08-31

### 修复

- 兼容豆包将“确认生成”渲染为普通 `div/span` 伪按钮的页面结构，按可见、可交互和严格动作文本定位并点击。
- 识别“确认后我再开始生成视频”“确认后开始生成视频”等生成前建议，不再把这类回复当成终止失败。
- 点击确认后继续等待正式的 Mini/Fast 提交状态和真实视频结果，不把确认动作本身当作生成成功。

### 验证

- `npm run test:unit` 通过，共 34 项测试。

## 0.1.47 - 2026-08-31

### 修复

- 过滤豆包时间、会话标题、模型入口等页面壳文字，避免 `今天 15:34 对话 豆包 快速` 被误报为最新回复。
- 增加 `已提交视频生成任务` 提交确认文案识别，确认已发出后继续等待结果，不重复提交或误退额度。
- 安全确认点击后忽略授权弹窗残留文字，只继续等待正式的 Mini/Fast 生成状态。
- 将“生成建议”归入提示词建议分支，按既有明确生成指令流程处理并保留原始回复。

### 验证

- `npm run typecheck` 通过。
- `npm run test:unit` 通过，共 34 项测试。

## 0.1.46 - 2026-08-31

### 修复

- 兼容豆包安全确认操作被渲染为 `div/span` 伪按钮的页面结构。
- 先定位完整授权文案容器，再仅点击容器内文本严格等于“确认”的可见控件，避免误点页面其他按钮。
- 通过真实参考图任务复测安全确认分支，失败时仍按原文返回并退回预扣额度。

## 0.1.45 - 2026-08-31

### 修复

- 识别豆包素材“安全确认”弹窗，只在弹窗包含授权文案且按钮为“确认”时点击，避免误点页面其他确认按钮。
- 记录安全确认点击动作，并继续等待正式 Mini/Fast 生成确认。
- 识别完成消息容器内的新版无扩展名视频预览图片、视频或画布节点，不再因找不到传统 `<video>` 标签而误判没有视频卡片。
- 准确对话在完成文案出现但卡片延迟渲染时持续等待，卡片出现后再定位分享入口。
- 增加安全确认和新版视频卡片识别回归测试。

## 0.1.44 - 2026-08-29

### 修复

- 根据近期真实失败报告扩展豆包相关回复识别，覆盖“需要我帮你”“需要我继续”“如果你需要”“我可以继续帮你”等提问和建议。
- 识别“可直接复制给 AI 视频工具”“AI 生成提示词”“负面提示词”“分镜脚本”“完整视频提示词”等改写结果，并自动追加一次明确生成指令。
- 明确违规和“暂时无法生成”回复仍保持终止，不会盲目重复提交。
- 增加近期真实错误回复回归测试。

## 0.1.43 - 2026-08-28

### 修复

- 视频结果恢复优先锁定任务提交时记录的准确豆包对话，持续等待分享资源同步 75 秒，不再过早遍历历史对话。
- 准确对话不可用或持续未同步时，历史恢复范围由最多 30 条收紧为同账号最近 3 条，并继续使用提示词指纹校验。
- 执行窗口标题固定显示账号名称和任务短 ID，避免并行任务观察时窗口难以区分。
- 操作日志增加准确对话重试次数及切换到最近对话的原因。

## 0.1.42 - 2026-08-28

### 修复

- 修复豆包把视频提示词改写成配音文案、分镜表或优化提示词后，程序直接判定失败的问题。
- 检测到提示词建议时，改为发送“请直接提交视频生成，不要改写、分析或扩写提示词。”并继续确认真实生成状态。
- 视频已生成但首次分享页尚未同步资源时，自动回到本次原会话恢复并重新复制分享链接，不再要求手动二次恢复。

## 0.1.41 - 2026-08-27

### 修复

- 豆包返回合规确认卡片时，自动识别并点击“确认生成”等二次确认按钮，再继续等待真实视频任务。
- 豆包先返回“要不要我再微调”“精简版提示词”“可以直接生成”等建议时，不再立即判定失败；无可用按钮时在同一对话回复一次“可以直接生成”，再按确认、生成中、完成和失败状态判断。
- 增加二次确认按钮和提示词建议回复的回归测试。

## 0.1.40 - 2026-08-27

### 修复

- 修复豆包页面导航栏、下载电脑版提示和页面时间被提取为“最新回复”的问题。
- 已确认提示词发出但没有真实失败回复时继续等待视频，不因页面 UI 噪声重复提交或退回额度。
- 增加页面 UI 噪声与用户消息拼接时的回归测试。

## 0.1.39 - 2026-08-14

### 修复

- 修复豆包提交确认文案被换行拆开时无法识别的问题。
- 已确认提示词发出但暂时没有标准确认文案时，任务进入已接受状态并继续等待视频结果，不重复提交、不退回预扣额度。
- 只有明确未发送或豆包明确返回失败时才执行失败和额度退回。
- 增加换行确认文案、换行失败文案的单元测试。

## 0.1.38 - 2026-08-13

### 修复

- 兼容豆包新版“正在为您生成一段… / 视频生成中 / 视频生成已提交”确认文案，避免已提交的视频被误判失败并退款。
- 确认文案必须是发送后新增内容，历史对话里的旧状态不会触发本次任务。
- 历史结果恢复现在会识别并回传豆包的侵权、违规等失败原文；明确显示“生成额度未扣除”时自动退回本地预扣额度。
- 等待超时但豆包仍显示“正在生成 / 视频生成中 / 视频生成已提交”时，任务保持后台生成状态，不再误报最终失败；用户可稍后手动恢复结果，不会重新提交。
- 保留 0.1.37 的繁忙重试和未知回复原文回传能力。

### 验证

- 增加豆包两套提交确认文案、生成中状态、违规优先级和繁忙回复反例测试；单元测试共 28 项。

## 0.1.37 - 2026-08-13

### 修复

- 只有出现对应 Seedance Mini/Fast 的正式提交确认，才进入视频生成等待；不再把输入框清空或普通助手回复误判为提交成功。
- 豆包返回“这会儿有点热闹”等繁忙提示时，自动等待 10 秒、30 秒重试，最多提交 3 次；仍未确认则自动退回预扣额度。
- 未识别的豆包回复会提取新增回复原文，写入接口失败原因和行动日志，排查时无需逐个打开账号窗口。
- 重试参考图任务时会重新进入视频模式、上传参考图、填写提示词并校验模型，避免重试丢失输入条件。

### 验证

- 增加豆包新增回复提取和繁忙提示识别单元测试。

## 0.1.36 - 2026-08-12

### 修复

- 修复当前页面为 Seedance 2.0 Fast 时没有真正切换到任务要求模型的问题。
- 模型切换改为真实坐标点击目标选项，点击后重新读取选中状态。
- 只有确认目标模型已选中后才允许提交，避免 Mini 任务误用 Fast。

## 0.1.35 - 2026-08-12

### 修复

- 修复任务要求 Mini 时沿用页面当前 Fast 模型的问题。
- 选择视频生成模式后先读取当前模型，再精确点击 Mini/Fast 目标项。
- 填写提示词后、发送前再次校验模型；模型未识别或与任务要求不一致时停止发送，不消耗生成额度。

## 0.1.30 - 2026-08-12

### 修复

- 视频完成文案不再单独触发分享，必须检测到本次任务新增的视频卡片后才进入分享流程。
- 兼容豆包使用 `video_dsz_watermark` 图片作为视频封面的页面结构。
- 分享入口优先限定在当前视频卡片内，避免误点页面顶部的对话级分享。
- 历史结果恢复等待视频卡片渲染完成后再判断，避免提示词已加载但视频卡片尚未出现时误报失败。
- 将“已记录豆包会话地址”和“已复制并验证视频分享地址”分开记录，避免日志产生误导。

### 验证

- 通过生产构建和页面状态单元测试。

## 0.1.29 - 2026-08-11

### 修复

- 针对第三方去水印服务的资源同步延迟，增加渐进式重试：立即、5 秒、15 秒、30 秒和 60 秒后解析。
- 去水印执行不再强制限制为 3 次；“未找到资源、处理中、获取失败”等暂时性错误会继续等待资源就绪。
- “平台暂不支持”等明确不可恢复错误仍立即失败，避免无意义重试。

### 验证

- 通过生产构建和 6 项去水印单元测试。

## 0.1.28 - 2026-08-10

### 修复

- 修复豆包视频已生成但复制分享链接失败的问题：顶部菜单定位改为识别 `...`，不再误点“下载电脑版”或播报按钮。
- 点击顶部菜单后必须确认出现“分享”菜单项，再进入分享面板并复制链接；错误弹窗会先关闭后再执行备用定位。
- 提交后等待并记录正式豆包对话地址，历史恢复优先使用当前对话，减少临时 `chat/local_...` 地址和重复提示词造成的误匹配。

### 验证

- 通过 TypeScript 类型检查、生产构建和 23 项单元测试。

## 0.1.27 - 2026-08-08

### 修复

- 当前会话确认视频已生成后直接进入分享面板，兼容豆包只渲染视频封面和播放按钮、尚未渲染 `<video>` 的成品卡片。
- 历史恢复只从豆包 `/chat/...` 原始对话复制，不再把 `/thread/...` 分享页继续嵌套分享。
- 复制分享链接后直接读取分享页，只有同时检测到视频完成文案、视频数据块和 MP4 资源才继续去水印。
- 分享面板存在部分勾选时自动执行全选，避免复制出不含视频的局部分享。
- 已确认包含视频的分享链接若解析失败，明确报告为第三方去水印服务未识别，不再误判为复制失败或反复复制同一链接。
- 去水印服务对有效分享链接持续返回失败时，将验证限制为 3 次短重试，避免单个任务无效等待 90 秒以上。
- 新增行动日志：逐步记录请求、账号、提交、参考图上传、分享地址复制、去水印解析和错误结果；分享地址可直接查看或复制。
- 行动日志默认保留 3 天，应用启动和写入日志时自动清理过期记录，避免本地数据库无限增长。

### 验证

- 使用已生成视频完成恢复流程回放，确认分享页包含 1280×720、10.08 秒 MP4 资源；单元测试共 23 项通过。

## 0.1.26 - 2026-08-07

### 修复

- 识别豆包"今日视频生成免费次数已用完"等失败文案，不再等待至提交确认或生成超时。
- 分享地址仍只作为去水印解析输入；只有去水印服务取得并验证真实可播放 MP4 后，任务才返回成功，避免把 `thread/...` 页面地址当成最终视频结果。
- 复制分享地址前后校验当前任务是否出现新的 HTTP 视频资源或可播放视频元素；只有分享地址和视频状态同时存在才送入去水印接口，避免把只有 `thread/...` 页面、没有视频的结果当成成功。
- 提交后记录当前豆包会话地址，结果恢复优先按会话地址查找，再按提示词特征扫描，降低系统默认对话名重复造成的误匹配。

### 验证

- 新增单元测试用例，全部通过。

## 0.1.25 - 2026-08-06

### 修复

- 识别豆包“下载电脑版 / 使用完整功能 / 下次提醒我”提示弹窗。
- 检测到该弹窗时自动点击“下次提醒我”完成标记，必要时再用关闭或 Esc 兜底，避免遮挡分享入口导致“未打开分享面板”。
- 在新对话、历史结果恢复和复制分享链接前统一处理该弹窗，减少生成成功但分享链接复制失败的情况。

### 验证

- 对最近两条失败任务进行了不重新生成的回放验证：其中一条已恢复真实 MP4，另一条确认没有完成态，仍按失败返回；新增弹窗识别测试，单元测试共 18 项通过。

## 0.1.24 - 2026-08-06

### 修复

- 修复豆包视频生成完成后，仍停留在等待状态或误匹配历史视频的问题；只识别本次任务新增的生成完成文案和视频资源。
- 增强提交确认和失败识别，覆盖违规/侵权内容、额度未扣除等豆包反馈，避免提交失败后继续等待。
- 适配豆包当前视频卡片的图标式分享工具栏，支持原生点击、DOM 兜底和剪贴板结果校验；并用全局互斥保护多账号并行复制链接。
- 优化历史结果恢复，按提示词相关度筛选对话，限制失效页面等待时间，减少恢复结果误匹配和长时间卡住。
- 去水印接口只有在取得并验证真实可播放 MP4 后才返回成功，不再把豆包聊天页、thread 页或分享页写入 `cleanVideoUrl`。

### 优化

- 去水印首次重试间隔从 5 秒缩短到 2.5 秒，重试等待上限从约 170 秒降到约 70.5 秒，保留 MP4/CDN 可播放校验。
- 去水印任务日志增加耗时、第几次解析和具体未就绪原因，便于区分上游准备延迟与真实失败。
- 回调按任务顺序在后台发送，并设置 5 秒超时，画布回调变慢时不再阻塞视频解析和账号调度。
- 完善账号池概览、额度、Mini/Fast 产能、状态筛选、接口日志详情和长提示词显示。
- 保持多账号独立窗口并行执行，同一账号任务顺序执行；支持参考图片上传、回调和历史结果恢复。

### 验证

- 通过 TypeScript 类型检查、生产构建和 17 项单元测试。
- 使用已有成功视频验证本地去水印接口：首次解析约 8.1 秒，第二次同地址约 0.28 秒，均返回可验证 MP4，未返回豆包分享页。

## 0.1.18 - 稳定版（历史版本）

> 此版本为此前已上传并保留的稳定版本。本次 `0.1.25` 不覆盖其本地安装包备份；当前 Git 仓库未重新创建不存在的 `v0.1.18` 标签。

## 0.1.14 - 2026-07-31

### 修复

- 识别豆包返回的“生成内容中疑似包含侵权 / 违规内容，无法返回该内容，换个主题再试试，生成额度未扣除”等失败文案，不再一直等待视频结果。
- 豆包明确提示额度未扣除时，自动退回本地预扣额度。
- 提示词填入改为多策略兜底：普通输入、剪贴板粘贴和 DOM 输入，并校验输入框确实包含本次提示词签名。
- 提交确认和失败文案改为只识别发送后新增内容，避免被历史对话里的旧确认或旧失败误判。
- 保留登录失效账号的 `login_required` 状态，不再被通用失败处理覆盖为空闲。

### 验证

- 增加豆包违规/侵权失败文案、额度未扣除、提示词签名和新增页面文案识别测试。

## 0.1.13 - 2026-07-31

### 修复

- 修复去水印服务对新分享链接短暂返回“未找到资源或获取失败”时过早判定任务失败的问题。
- 去水印渐进重试窗口由约 26 秒延长到约 3 分钟，并为单次请求增加 20 秒超时。
- 将“解析失败”“资源处理中”“稍后重试”等临时状态纳入自动重试范围。
- 重试期间保持任务为“生成中”，日志显示下次重试时间和当前次数，不再让画布过早收到失败结果。
- 修复所有账号共用单一全局执行队列、导致多账号任务仍然串行的问题。
- 不同账号可分别打开独立执行窗口并行生成；同一账号仍保持单任务顺序执行。
- “最大并发账号数”正式接入调度器，旧版默认值从 1 一次性迁移为 4。
- 修复 macOS ad-hoc 签名误开 Hardened Runtime 后，安装版无法加载 Electron Framework 的启动问题。

### 验证

- 增加去水印结果提取、MP4 地址识别、临时错误重试、不支持平台终止和多账号并发调度测试。

## 0.1.12 - 2026-07-31

### 修复

- 修复 macOS 应用包只有 linker 临时签名、严格校验失败的问题。
- 构建 macOS 安装包时自动对主程序、Helper、Framework 和原生模块执行完整 ad-hoc 签名。
- 发布前增加 `codesign --deep --strict` 校验，降低下载后被误报为“应用已损坏”的概率。

## 0.1.11 - 2026-07-31

### 新增

- 提供 macOS Apple Silicon DMG 和 Windows x64 EXE 安装包。
- 增加 Windows NSIS 标准安装向导，可选择安装目录并创建桌面、开始菜单快捷方式。
- 增加账号池总剩余额度以及 Mini、Fast 预计可生成次数。
- 增加账号搜索、可用状态筛选和批量操作菜单。

### 优化

- 将账号、登录、可用、耗尽、总额度和模型产能压缩为单行概览。
- 合并登录状态与运行状态，减少重复信息。
- 额度列改为剩余值、消耗进度和模型可生成次数。
- 行内操作精简为“打开豆包”“检测”和更多菜单。
- 统一蓝色主视觉，压缩 API 状态区和页面无效留白。

## 0.1.10 - 2026-07-31

- 增加账号池总额度、已消耗额度和 Mini/Fast 产能估算。
- 产能按每个账号分别取整后汇总，避免跨账号合并额度造成误算。

## 0.1.9 - 2026-07-31

- 重构账号池首页，增加账号概览、搜索和状态筛选。
- 合并账号登录与运行状态。
- 将设置、额度重置、清空登录和删除收纳进更多菜单。

## 0.1.8 - 2026-07-31

- 重构接口日志为固定列宽摘要表。
- 提示词支持三行预览和完整详情弹窗。
- 增加日志搜索、状态筛选、提示词复制和最终结果复制。
- 长提示词和长视频地址不再撑开表格。

## 0.1.7 - 2026-07-30

- 增加去水印结果重试和历史任务结果恢复。
- 强化 MP4 地址验证，只有真实视频结果才返回成功。
- 回调结果不再把豆包分享页或会话页写入 `cleanVideoUrl`。

## 0.1.0 - 2026-07-30

- 完成 Electron、Vue 3、TypeScript、SQLite 和 electron-builder 项目骨架。
- 支持多个独立 partition、持久化登录和账号状态管理。
- 提供本地生成接口、基础额度扣减和接口日志。
