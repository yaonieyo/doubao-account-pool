# Development State

当前目标是稳定豆包多账号视频接口：可靠提交 Mini/Fast 和目标画幅，识别安全确认、建议回复及异步结果，最终只返回经验证的去水印 MP4。当前发布版本推进为 `0.1.72`；`0.1.66` 已将分享链接改为页面内拦截 `navigator.clipboard.write`/`writeText`，从 `ClipboardItem` 提取并校验 `thread/share` 地址，不再读写 Electron 进程级系统剪贴板；`0.1.67` 增加豆包内部滚动和卡片真实点击；`0.1.68` 扩展 `block-video`/`video-card` 卡片定位；`0.1.69` 先按提示词相关性排序，再取 10 条历史对话候选，只用于恢复，不会重新提交；`0.1.70` 增加后台真实悬停视频卡片，并优先定位新版视频操作栏外层的稳定分享图标；`0.1.71` 允许动态渲染分享页先通过 thread/share 地址检查，最终成功仍必须由去水印接口返回并验证可播放 MP4。已确认类型检查、构建和 40 项单元测试通过，并已替换本机 17888 服务。2026-09-23 已更新并验证新的去水印配置，账号 003 的 6 条历史任务已恢复为成功并取得经验证的 MP4 地址；随后已将这 6 个 H.264 MP4 下载到无限画布 `assets/output` 并回填“ 四个周期 ”画布，5 个写入原视频节点，片段 1 因原视频子节点缺失而补建节点，画布 API 读回和 `ffprobe` 均验证通过。`doubao-f26d4518d31d43aa` 曾两次进入恢复队列但仍失败：任务没有记录正式豆包会话地址，最近 10 条历史会话提示词匹配为 0，原始视频地址、去水印地址和本地文件均为空；因此不能安全串入其它任务，也没有内容可回传画布。用户确认其在豆包侧手动补发了生成指令并看到了视频结果，但当前后台没有保存该对话身份；下一次遇到同类情况必须保留准确的 `chat/thread` 地址，或先绑定该地址再恢复，不应仅凭账号、时间或相似提示词猜测。画布端当前采用任务轮询而非回调，历史任务结束后不会自动把后来恢复的结果写回节点；本次已通过画布 API 做了带备份的定向回填。下一步是发布 `0.1.72` 并让画布按 `requestId` 轮询或接收回调；无正式会话地址的任务仍需增加明确的会话绑定/恢复入口，继续保持 MP4 验证后才返回成功。
